void setup()
{
    // Set the serial monitor baudrate to 9600
    Serial.begin(9600);
}
 
void loop()
{
    int level;
    level = analogRead(0);
    Serial.println("Analog value:");
    Serial.println(level);
  delay(1000);
}
